# BibleQuest V6 Database CI

Status: executable V6 database-CI foundation
Architecture: ADR-0002

## Canonical clean-bootstrap model

BibleQuest V5 shipped with a checked-in `supabase/schema.sql` snapshot that represents the released database baseline, while the older `supabase/migrations/` directory is not a valid standalone Supabase replay chain. In particular, multiple historical files share date-only migration versions such as `20260904`, which Supabase correctly rejects as duplicate migration versions.

V6 therefore uses an explicit **released-V5 baseline + V6-forward-migrations** model.

The disposable CI project:

1. copies `supabase/schema.sql` to `00000000000000_biblequest_v5_release_baseline.sql`;
2. does **not** replay pre-V6 historical SQL that is already represented by that released baseline;
3. copies only migrations newer than the V5 release cutoff;
4. requires every V6 forward migration to use a unique 14-digit timestamp prefix;
5. applies deterministic test data from `supabase/seed-v6-ci.sql`;
6. runs pgTAP suites from `supabase/tests/v6-*.test.sql`;
7. discards the local database.

Historical pre-V6 migration files remain in the repository as audit evidence. They are not rewritten or renamed merely to satisfy CI.

The V5 baseline snapshot must match the released BibleQuest database contract. During this tranche, live read-only verification confirmed that production contains `private.bible_role_in_congregation(uuid)` with authenticated execute and no anon execute; the checked-in snapshot omitted that released helper, so the baseline snapshot was corrected. Production was not mutated.

## Current proof surface

The first suite verifies:

- RLS remains enabled on assignments, shared calendar events and congregation memberships;
- anonymous users do not receive assignment-table access;
- authenticated users retain the intended assignment read grant;
- browser roles cannot read the server-only Admin audit log;
- the bounded membership SECURITY DEFINER remains executable only by authenticated users and declares an explicit search path;
- Member A sees only Congregation A assignments/calendar/membership rows;
- Member B sees only Congregation B assignments/calendar/membership rows;
- adding a foreign congregation filter cannot bypass RLS.

Fixtures additionally include a Leader, an Admin and an isolated platform Owner identity for expansion of the role matrix.

## Migration rules for V6

All new V6 migrations must:

- use a unique 14-digit timestamp prefix, e.g. `20260919143000_description.sql`;
- sort after the V5 baseline cutoff;
- be additive/idempotent where practical;
- preserve RLS/grants and fail closed;
- pass clean baseline-to-current replay in disposable CI before integration.

Do not rename or edit old applied migration versions merely to manufacture a green historical replay.

## Safety

This CI path requires no hosted Supabase project and no production credentials. The workflow intentionally contains no `supabase link`, `--linked`, access token, database password, or service-role secret.

The local Supabase stack uses default local-only credentials generated by the CLI. Do not expose the local stack to external networks.

## Remaining Phase-2 expansion

- add write allow/deny tests for member, Leader and Admin paths;
- add real function grant/revoke and SECURITY INVOKER/DEFINER matrices across sensitive functions;
- cover notification/push, presence, teams/groups and ministry/admin domains;
- commit a reviewed generated DB type snapshot and turn deterministic generation into a drift failure;
- test supported database upgrade paths separately from clean install;
- expand fixtures if the congregation-level `pastor` role contract is made schema-consistent.
