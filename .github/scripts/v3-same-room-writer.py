from pathlib import Path

BASE='ab3584906b3d017ea555416910f23e9414ed2ef8'

def once(text, old, new, label):
    count=text.count(old)
    if count!=1:
        raise SystemExit(f'{label}: expected 1 anchor, found {count}')
    return text.replace(old,new,1)

p=Path('src/app/games.js'); text=p.read_text()
text=once(text,"const normalizeAnswer=value=>String(value||'').trim().toLocaleLowerCase();","const normalizeAnswer=value=>String(value||'').trim().toLocaleLowerCase();\nconst SAME_ROOM_MIN=2,SAME_ROOM_MAX=6;\nconst emptySameRoom=()=>({phase:'same-room-setup',players:[],bank:[],index:0,currentPlayerIndex:0,locked:false,selected:null,correct:null});",'games constants')
text=once(text,"  let state=emptyState();","  let state=emptyState();\n  let sameRoom=emptySameRoom();",'games state')
anchor="  function persistResult(mode,score,total,gained){const result={score,total,gained,completedAt:completionTime()};results={...results,[mode]:result};storage.write(RESULTS_KEY,results);return result}\n"
same_room='''\n  function sameRoomSnapshot(){\n    const question=sameRoom.phase==='same-room-question'?sameRoom.bank[sameRoom.index]||null:null;\n    const players=Object.freeze(sameRoom.players.map((player,index)=>Object.freeze({...player,active:sameRoom.phase==='same-room-question'&&index===sameRoom.currentPlayerIndex})));\n    return Object.freeze({phase:sameRoom.phase,players,index:sameRoom.index,total:sameRoom.bank.length,currentPlayerIndex:sameRoom.currentPlayerIndex,currentPlayer:players[sameRoom.currentPlayerIndex]||null,question:freezeQuestion(question),locked:sameRoom.locked,selected:sameRoom.selected,correct:sameRoom.correct});\n  }\n\n  function startSameRoom(playerCount=2){\n    const count=Number(playerCount);\n    if(!Number.isInteger(count)||count<SAME_ROOM_MIN||count>SAME_ROOM_MAX)throw new Error('Play Together requires 2 to 6 players.');\n    const base=[...buildGameRound('mixed-quest')],bank=moderation?[...moderation.applyCore(base)]:base;\n    if(!bank.length)throw new Error(moderation?'Play Together has no questions available under the current content policy.':'Play Together has no verified questions.');\n    sameRoom={phase:'same-room-question',players:Array.from({length:count},(_,index)=>({id:`player-${index+1}`,name:`Player ${index+1}`,score:0})),bank,index:0,currentPlayerIndex:0,locked:false,selected:null,correct:null};\n    return sameRoomSnapshot();\n  }\n\n  function answerSameRoom(choiceIndex){\n    if(sameRoom.phase!=='same-room-question')throw new Error('Start Play Together before answering.');\n    if(sameRoom.locked)return Object.freeze({applied:false,duplicate:true,...sameRoomSnapshot()});\n    const question=sameRoom.bank[sameRoom.index],choice=Number(choiceIndex);\n    if(!Number.isInteger(choice)||choice<0||choice>=question.choices.length)throw new Error('Choose one of the available answers.');\n    const correct=choice===question.answer,players=sameRoom.players.map((player,index)=>index===sameRoom.currentPlayerIndex?{...player,score:player.score+(correct?1:0)}:player);\n    sameRoom={...sameRoom,players,locked:true,selected:choice,correct};\n    return Object.freeze({applied:true,duplicate:false,...sameRoomSnapshot()});\n  }\n\n  function nextSameRoom(){\n    if(sameRoom.phase!=='same-room-question')throw new Error('There is no active Play Together question.');\n    if(!sameRoom.locked)throw new Error('Answer the current Play Together question before continuing.');\n    if(sameRoom.index+1>=sameRoom.bank.length){sameRoom={...sameRoom,phase:'same-room-complete',index:sameRoom.bank.length,locked:false,selected:null,correct:null};return sameRoomSnapshot()}\n    sameRoom={...sameRoom,index:sameRoom.index+1,currentPlayerIndex:(sameRoom.currentPlayerIndex+1)%sameRoom.players.length,locked:false,selected:null,correct:null};\n    return sameRoomSnapshot();\n  }\n\n  function finishSameRoom(){\n    if(sameRoom.phase==='same-room-complete')return sameRoomSnapshot();\n    if(sameRoom.phase!=='same-room-question')throw new Error('Start Play Together before finishing.');\n    sameRoom={...sameRoom,phase:'same-room-complete',locked:false,selected:null,correct:null};return sameRoomSnapshot();\n  }\n  function resetSameRoom(){sameRoom=emptySameRoom();return sameRoomSnapshot()}\n'''
text=once(text,anchor,anchor+same_room,'games methods')
text=once(text,"  function showLauncher(){state=emptyState();return snapshot()}","  function showLauncher(){sameRoom=emptySameRoom();state=emptyState();return snapshot()}",'games cleanup')
old="return Object.freeze({getState:snapshot,modes:Object.freeze(ALL_MODES.map(mode=>Object.freeze({...mode}))),start,answer,next,replay,startDetective,answerDetective,replayDetective,startTimeline,moveTimeline,checkTimeline,replayTimeline,openRecallLibrary,setRecallQuery,visibleRecallBooks,startRecallBook,revealRecall,rateRecall,recallSummary,recallReviewQueue,syncRecallReviewItem,returnRecallLibrary,replayRecall,showLauncher,lastResult,leave,xp:XP});"
new="return Object.freeze({getState:snapshot,modes:Object.freeze(ALL_MODES.map(mode=>Object.freeze({...mode}))),start,answer,next,replay,startDetective,answerDetective,replayDetective,startTimeline,moveTimeline,checkTimeline,replayTimeline,openRecallLibrary,setRecallQuery,visibleRecallBooks,startRecallBook,revealRecall,rateRecall,recallSummary,recallReviewQueue,syncRecallReviewItem,returnRecallLibrary,replayRecall,showLauncher,lastResult,leave,getSameRoomState:sameRoomSnapshot,startSameRoom,answerSameRoom,nextSameRoom,finishSameRoom,resetSameRoom,sameRoomLimits:Object.freeze({min:SAME_ROOM_MIN,max:SAME_ROOM_MAX}),xp:XP});"
p.write_text(once(text,old,new,'games api'))

p=Path('src/features/games/index.js'); text=p.read_text()
old="}).join('')}</section><div class=\"bq-game-footer\"><button type=\"button\" class=\"bq-secondary-button\" data-game-home>Back home</button></div>`;return;"
new="}).join('')}<article class=\"bq-panel bq-game-card\" data-same-room-card><span>PASS-AND-PLAY</span><h2>Play Together</h2><p>Share one device with 2–6 players, rotate turns, and keep a local scoreboard.</p><button type=\"button\" class=\"bq-primary-button\" data-same-room-open>Play Together</button></article></section><div class=\"bq-game-footer\"><button type=\"button\" class=\"bq-secondary-button\" data-game-home>Back home</button></div>`;return;"
text=once(text,old,new,'ui launcher')
anchor="        if(state.phase==='detective'){"
branches='''        if(state.phase==='same-room-setup'){\n          host.innerHTML=`<section class="bq-panel bq-games-head" data-same-room-setup><p class="bq-eyebrow">PLAY TOGETHER</p><h1>How many players?</h1><p>Pass this device after each question. Scores stay local to this session and do not award profile XP.</p><div class="bq-game-actions">${[2,3,4,5,6].map(count=>`<button type="button" class="bq-primary-button" data-same-room-start="${count}">${count} players</button>`).join('')}</div><button type="button" class="bq-secondary-button" data-game-launcher>All games</button></section>`;return;\n        }\n        if(state.phase==='same-room-question'){\n          const q=state.question,progressPct=Math.round((state.index/Math.max(1,state.total))*100);\n          host.innerHTML=`<section class="bq-game-topline"><button type="button" class="bq-secondary-button" data-game-launcher>All games</button><div class="bq-game-score" data-same-room-turn>Turn: <b>${escapeHtml(state.currentPlayer.name)}</b></div></section><section class="bq-panel bq-question-card" data-same-room-question="${escapeHtml(q.id)}"><p class="bq-eyebrow">PLAY TOGETHER · QUESTION ${state.index+1} OF ${state.total}</p><div class="bq-game-progress" aria-hidden="true"><i style="width:${progressPct}%"></i></div><div class="bq-game-stats" data-same-room-scoreboard>${state.players.map(player=>`<div data-same-room-player="${escapeHtml(player.id)}"><b>${player.score}</b><span>${escapeHtml(player.name)}${player.active?' · turn':''}</span></div>`).join('')}</div><h1>${escapeHtml(q.q)}</h1><div class="bq-game-choices">${q.choices.map((choice,index)=>{const isCorrect=state.locked&&index===q.answer,isWrong=state.locked&&index===state.selected&&!state.correct,className=isCorrect?' is-correct':isWrong?' is-wrong':'';return `<button type="button" class="bq-game-choice${className}" data-same-room-answer="${index}" ${state.locked?'disabled':''}><span aria-hidden="true">${String.fromCharCode(65+index)}</span><b>${escapeHtml(choice)}</b></button>`}).join('')}</div>${state.locked?`<div class="bq-game-explanation" data-same-room-feedback><strong>${state.correct?'Correct':'Review this one'}</strong><p>${escapeHtml(q.why)}</p><span>📖 ${escapeHtml(q.ref)}</span></div><button type="button" class="bq-primary-button" data-same-room-next>${state.index+1===state.total?'See final scoreboard':'Pass to next player'}</button>`:''}<div class="bq-game-footer"><button type="button" class="bq-secondary-button" data-same-room-finish>Finish game</button></div>${GAME_SOURCE}</section>`;return;\n        }\n        if(state.phase==='same-room-complete'){\n          const top=Math.max(...state.players.map(player=>player.score)),winners=state.players.filter(player=>player.score===top).map(player=>player.name),result=winners.length===1?`Winner: ${winners[0]}`:`Tie: ${winners.join(' & ')}`;\n          host.innerHTML=`<section class="bq-panel bq-game-result" data-same-room-complete><p class="bq-eyebrow">PLAY TOGETHER COMPLETE</p><h1>${escapeHtml(result)}</h1><div class="bq-game-stats" data-same-room-scoreboard>${state.players.map(player=>`<div data-same-room-player="${escapeHtml(player.id)}"><b>${player.score}</b><span>${escapeHtml(player.name)}</span></div>`).join('')}</div><div class="bq-game-actions"><button type="button" class="bq-primary-button" data-same-room-open>Play again</button><button type="button" class="bq-secondary-button" data-game-launcher>All games</button></div></section>`;return;\n        }\n'''
text=once(text,anchor,branches+anchor,'ui phases')
anchor="          if(target.closest('[data-detective-replay]')){render(games.replayDetective());return}"
handlers="""          if(target.closest('[data-same-room-open]')){render(games.getSameRoomState());return}\n          const sameStart=target.closest('[data-same-room-start]');if(sameStart){render(games.startSameRoom(sameStart.dataset.sameRoomStart));return}\n          const sameAnswer=target.closest('[data-same-room-answer]');if(sameAnswer){render(games.answerSameRoom(sameAnswer.dataset.sameRoomAnswer));return}\n          if(target.closest('[data-same-room-next]')){render(games.nextSameRoom());return}\n          if(target.closest('[data-same-room-finish]')){render(games.finishSameRoom());return}\n"""
p.write_text(once(text,anchor,handlers+anchor,'ui handlers'))

Path('tests/v3-same-room-play-together-edge.mjs').write_text("""import assert from 'node:assert/strict';
import { createGameLauncherService } from '../src/app/games.js';
const events=[];let writes=0;const progress={record(event){events.push(event);return{applied:true}}};const storage={read(_key,fallback=null){return fallback},write(_key,value){writes+=1;return value}};const recall={async loadManifest(){return{books:[],source:'test',license:'test'}},async loadBook(){throw new Error('unused')}};
const games=createGameLauncherService({progress,storage,recall,roundIdFactory:(mode,seq)=>`${mode}-${seq}`,clock:()=>new Date('2026-09-11T00:00:00Z')});
assert.deepEqual(games.sameRoomLimits,{min:2,max:6});assert.throws(()=>games.startSameRoom(1),/2 to 6/);assert.throws(()=>games.startSameRoom(7),/2 to 6/);
let state=games.getSameRoomState();assert.equal(state.phase,'same-room-setup');state=games.startSameRoom(3);assert.equal(state.players.length,3);assert.equal(state.currentPlayer.name,'Player 1');assert.equal(state.total,10);assert.equal(state.players.every(player=>player.score===0),true);
state=games.answerSameRoom(state.question.answer);assert.equal(state.locked,true);assert.equal(state.correct,true);assert.equal(state.players[0].score,1);const duplicate=games.answerSameRoom(state.question.answer);assert.equal(duplicate.duplicate,true);assert.equal(duplicate.players[0].score,1);
state=games.nextSameRoom();assert.equal(state.currentPlayer.name,'Player 2');assert.equal(state.index,1);const wrong=(state.question.answer+1)%state.question.choices.length;state=games.answerSameRoom(wrong);assert.equal(state.correct,false);assert.equal(state.players[1].score,0);state=games.nextSameRoom();assert.equal(state.currentPlayer.name,'Player 3');assert.equal(state.index,2);
state=games.finishSameRoom();assert.equal(state.phase,'same-room-complete');assert.deepEqual(state.players.map(player=>player.score),[1,0,0]);assert.equal(events.length,0,'Same-room local scores must not award profile XP.');assert.equal(writes,0,'Same-room local session must not persist data.');
state=games.resetSameRoom();assert.equal(state.phase,'same-room-setup');state=games.startSameRoom(6);assert.equal(state.players.length,6);games.showLauncher();assert.equal(games.getSameRoomState().phase,'same-room-setup');
console.log('BibleQuest v3 Same-room Play Together edge regression passed');
""")
Path('tests/v3-same-room-play-together-smoke.mjs').write_text("""import { chromium } from 'playwright';
const BASE=process.env.BQ_BASE_URL||'http://127.0.0.1:4173/';const browser=await chromium.launch({headless:true});const assert=(condition,message)=>{if(!condition)throw new Error(message)};
try{const page=await browser.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true});const errors=[];page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});page.on('pageerror',e=>errors.push(e.message));await page.goto(BASE,{waitUntil:'networkidle'});await page.locator('[data-route-link=\"play\"]').click();await page.waitForURL(/#\\/play$/);await page.locator('[data-same-room-open]').click();await page.locator('[data-same-room-setup]').waitFor();assert(await page.locator('[data-same-room-start]').count()===5,'Play Together must offer 2–6 players.');await page.locator('[data-same-room-start=\"3\"]').click();await page.locator('[data-same-room-question=\"q1\"]').waitFor();assert(await page.locator('[data-same-room-player]').count()===3,'Three-player scoreboard missing.');assert((await page.locator('[data-same-room-turn]').textContent())?.includes('Player 1'),'Player 1 must start.');await page.locator('[data-same-room-answer=\"1\"]').click();await page.locator('[data-same-room-feedback]').waitFor();assert((await page.locator('[data-same-room-player=\"player-1\"] b').textContent())==='1','Player 1 score did not increment.');await page.locator('[data-same-room-next]').click();assert((await page.locator('[data-same-room-turn]').textContent())?.includes('Player 2'),'Turn did not rotate to Player 2.');await page.locator('[data-same-room-answer=\"0\"]').click();await page.locator('[data-same-room-next]').click();assert((await page.locator('[data-same-room-turn]').textContent())?.includes('Player 3'),'Turn did not rotate to Player 3.');await page.locator('[data-same-room-finish]').click();await page.locator('[data-same-room-complete]').waitFor();assert(await page.locator('[data-same-room-player]').count()===3,'Final scoreboard missing players.');const metrics=await page.evaluate(()=>({innerWidth,scrollWidth:document.documentElement.scrollWidth,minTarget:Math.min(...[...document.querySelectorAll('[data-games-page] button')].map(node=>node.getBoundingClientRect().height))}));assert(metrics.scrollWidth<=metrics.innerWidth+1,'Play Together mobile overflow.');assert(metrics.minTarget>=44,'Play Together touch target below 44px.');assert(errors.length===0,`Unexpected console/page errors: ${errors.join(' | ')}`);await page.close();console.log('BibleQuest v3 Same-room Play Together browser regression passed.')}finally{await browser.close()}
""")
Path('scripts/validate-v3-same-room-play-together.mjs').write_text("""import assert from 'node:assert/strict';import fs from 'node:fs';
const service=fs.readFileSync('src/app/games.js','utf8'),ui=fs.readFileSync('src/features/games/index.js','utf8'),workflow=fs.readFileSync('.github/workflows/v3-regression.yml','utf8'),inventory=fs.readFileSync('FEATURE_INVENTORY_V3.md','utf8');
for(const token of ['getSameRoomState','startSameRoom','answerSameRoom','nextSameRoom','finishSameRoom','resetSameRoom','SAME_ROOM_MIN=2','SAME_ROOM_MAX=6'])assert.ok(service.includes(token),`Missing Same-room owner contract: ${token}`);for(const token of ['data-same-room-open','data-same-room-start','data-same-room-scoreboard','data-same-room-answer','data-same-room-next','data-same-room-finish','same-room-complete'])assert.ok(ui.includes(token),`Missing Same-room UI contract: ${token}`);
assert.equal(/progress\\.record\\(/.test(service.slice(service.indexOf('function startSameRoom'),service.indexOf('function resetSameRoom'))),false,'Same-room score path must not award profile XP.');assert.ok(inventory.includes('| 42 | Same-room Play Together | 2–6 players; rotating turns; scoreboard; finish |'));for(const file of ['scripts/validate-v3-same-room-play-together.mjs','tests/v3-same-room-play-together-edge.mjs','tests/v3-same-room-play-together-smoke.mjs'])assert.ok(workflow.includes(file),`Regression workflow missing ${file}`);console.log('BibleQuest v3 Same-room Play Together architecture validation passed');
""")
Path('docs/V3_SAME_ROOM_PLAY_TOGETHER_CONTRACT.md').write_text("""# BibleQuest v3 Same-room Play Together contract

Milestone #42 restores local pass-and-play only: 2–6 players, rotating turns, an in-session scoreboard, and a finish state.

Ownership remains in `src/app/games.js`; `src/features/games/index.js` renders and delegates. Same-room scores are ephemeral, do not persist, and do not award profile XP. This milestone does not add networking, room IDs, realtime presence, cross-device play, or any #43 Live Rooms behavior.
""")
p=Path('.github/workflows/v3-regression.yml'); text=p.read_text();text=once(text,'scripts/validate-v3-reset-recovery.mjs; do','scripts/validate-v3-reset-recovery.mjs scripts/validate-v3-same-room-play-together.mjs; do','workflow validator');text=once(text,'tests/v3-reset-recovery-edge.mjs tests/v3-progress-edge.mjs','tests/v3-reset-recovery-edge.mjs tests/v3-same-room-play-together-edge.mjs tests/v3-progress-edge.mjs','workflow edge');text=once(text,'tests/v3-reset-recovery-smoke.mjs tests/v3-progress-smoke.mjs','tests/v3-reset-recovery-smoke.mjs tests/v3-same-room-play-together-smoke.mjs tests/v3-progress-smoke.mjs','workflow smoke');p.write_text(text)
