import fs from 'node:fs';
const read=path=>fs.readFileSync(path,'utf8'),failures=[],fail=message=>failures.push(message);
const required=['src/app/assignments.js','src/features/assignments/index.js','ASSIGNMENTS_V3.md','tests/v3-assignments-edge.mjs','tests/v3-assignments-smoke.mjs','src/core/api.js','src/app/bootstrap.js','src/features/community/index.js','.github/workflows/v3-regression.yml','FEATURE_INVENTORY_V3.md'];
for(const file of required)if(!fs.existsSync(file))fail(`Missing Assignments v3 file: ${file}`);
const owner=read('src/app/assignments.js'),ui=read('src/features/assignments/index.js'),api=read('src/core/api.js'),bootstrap=read('src/app/bootstrap.js'),community=read('src/features/community/index.js'),contract=read('ASSIGNMENTS_V3.md'),workflow=read('.github/workflows/v3-regression.yml'),inventory=read('FEATURE_INVENTORY_V3.md');
for(const token of['export function createAssignmentsService','api.load','api.start','api.complete','api.subscribe','congregation.assert','BQ_ASSIGNMENT_SCOPE','BQ_ASSIGNMENT_RESPONSE','BQ_ASSIGNMENT_NOT_FOUND','BQ_ASSIGNMENT_ROLE_READ_ONLY','submissionMax:4000','stopSync','watch(listener)'])if(!owner.includes(token))fail(`Assignments owner missing contract: ${token}`);
if(/document\.|window\.|localStorage|sessionStorage|createClient|\.from\(|functions\.invoke|\.channel\(/.test(owner))fail('Assignments owner must remain DOM/storage/Supabase/Realtime-implementation independent.');
if(/localStorage|sessionStorage|createClient|\.from\(|functions\.invoke|\.channel\(|bible_assignment_progress|bible_assignments/.test(ui))fail('Assignments UI bypasses the Assignments/API ownership boundary.');
for(const token of["'bible_assignments'","'bible_assignment_progress'","'bq-assignment'","action:'start'","action:'complete'",'client.channel','removeChannel',".eq('user_id',userId)"])if(!api.includes(token))fail(`Central API missing Assignments boundary: ${token}`);
if(/\.insert\([^\n]*bible_assignment_progress|\.update\([^\n]*bible_assignment_progress|\.upsert\([^\n]*bible_assignment_progress/.test(api))fail('Browser API must not directly mutate assignment progress; retained trusted function owns writes.');
for(const token of['createAssignmentsService','assignmentsPage','api.assignments',"assignments:()=>assignmentsPage",'assignments.clear'])if(!bootstrap.includes(token))fail(`Bootstrap missing Assignments composition: ${token}`);
if(!community.includes('data-community-route="assignments"'))fail('Community must expose the Assignments route.');
for(const phrase of['#73 Assignments','bq-assignment','RLS','realtime','read-only for ministry roles','#74 Advanced assignments','#75 Assignment push workflow','#79 Linked activities/challenges'])if(!contract.includes(phrase))fail(`Assignments contract missing boundary: ${phrase}`);
const row=n=>inventory.split('\n').find(line=>line.startsWith(`| ${n} |`))||'';
if(!/\| (Implemented|Verified|Regression-tested) \|/.test(row(73)))fail('Inventory #73 must be Implemented or better once this validator is accumulated.');
if(!/\| (Implemented|Verified|Regression-tested) \|/.test(row(74))&&!/\| Not started \|/.test(row(74)))fail('Inventory #74 must use a valid lifecycle state while extending the existing Assignments owner.');
if(!/\| (Implemented|Verified|Regression-tested) \|/.test(row(75))&&!/\| Not started \|/.test(row(75)))fail('Inventory #75 must use a valid lifecycle state while extending the existing Assignments owner.');
if(!/\| (Implemented|Verified|Regression-tested) \|/.test(row(79))&&!/\| Not started \|/.test(row(79)))fail('Inventory #79 must use a valid lifecycle state while extending the existing Assignments owner.');
for(const token of['scripts/validate-v3-assignments.mjs','tests/v3-assignments-edge.mjs','tests/v3-assignments-smoke.mjs'])if(!workflow.includes(token))fail(`Accumulated workflow missing Assignments coverage: ${token}`);
if(failures.length){console.error(`BibleQuest v3 Assignments validation FAILED (${failures.length})`);failures.forEach(message=>console.error(`- ${message}`));process.exit(1)}
console.log('BibleQuest v3 Assignments architecture validation passed.');