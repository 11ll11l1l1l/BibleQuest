# BibleQuest v3 — 6:00 PM JST release control

Date: 2026-09-11
Deadline: **18:00 JST**
Canonical production target: **https://mybiblequest.pages.dev/**
Compatibility target: **https://biblequest-7th.pages.dev/**

This file is the overriding execution priority until the 2026-09-11 production release is complete. If any older handoff, milestone, polish plan, agent instruction, or backlog item conflicts with this deadline plan, this file wins.

## Objective

Have a verified BibleQuest v3 release available on the existing Cloudflare Pages production website by 18:00 JST today.

The Cloudflare projects already deploy repository `main`. Production promotion therefore means: produce an exact verified release candidate, promote that exact product state to `main`, allow the existing Cloudflare Pages projects to build/deploy it, then verify the deployed production identity and basic runtime behavior.

## Scope freeze

- Active v3 release scope is complete at 98/98 applicable capabilities.
- Historical rows #39 Hiragana Match and #40 Kids Bible Who Am I are user-retired from this release and are not blockers.
- No new features before the 18:00 release.
- No new Kids/Kana games before release.
- Artwork/theme work is optional and may proceed only when it is low-risk, replacement-level polish that does not alter information architecture, navigation, feature ownership, persistence, backend contracts, or core interaction flows.
- Stop artwork/theme changes as soon as they threaten the validation/deployment window.
- Fix only reproduced P0/P1 release defects. P2/P3 cosmetic defects that do not break core use are documented for post-release work unless trivially safe.

## Read-only release agents

All five support agents are retasked for the 18:00 release and must follow `RELEASE_AGENT_READONLY_2026-09-11.md`.

They are **investigators only**. They may inspect code/configuration, diffs, branches, tests, workflow evidence, and production behavior when available, but they may not create or modify code, documentation, branches, commits, PRs, issues, workflow state, Cloudflare, Supabase, DNS, secrets, data, or production configuration. They do not deploy and they do not apply fixes.

Their assignments are intentionally separated:

1. Cloudflare/deployment investigator.
2. Regression/browser/mobile/PWA investigator.
3. Core product smoke investigator.
4. Security/backend-boundary investigator.
5. Release firewall/triage investigator.

Their only deliverable is concise evidence/findings to the captain/release executor. Credible P0/P1 findings must include the exact inspected SHA, reproduction or concrete evidence, user impact, and recommended minimal captain action. P2/cosmetic/backlog findings must not distract the release train.

The captain/release-execution chat remains the only writer and is responsible for validating findings, making fixes, running gates, freezing the release candidate, promoting to `main`, and verifying Cloudflare.

## Release train

### Phase A — release preparation and blocker audit
Target: immediately

1. Start from `feature/v3-post-parity-closeout`, based on frozen verified `release/v3.71-japanese-furigana` (`c631bea8d5177a9a2ff68139cb104b6fbf26015b`).
2. Treat v3.71 run `34550650269` as evidence only for that frozen SHA; changed product SHAs must earn their own PASS.
3. Confirm Cloudflare compatibility: `build.sh`, `scripts/deploy-gate.mjs`, static entry points, `_headers`/routing behavior, service worker/PWA assets, and production-relative paths.
4. Reconcile any stale release documentation that still assumes the v2/main architecture.
5. Identify only blockers that can prevent a safe production deployment today.

### Phase B — release hardening / limited polish
Target completion: **14:30 JST**

- Correct reproduced P0/P1 defects only.
- Permitted polish: artwork/icon/color/asset replacement and visual consistency changes that do not redesign layouts or behavior.
- Every product change requires corresponding focused regression protection when appropriate.
- At 14:30 JST, freeze discretionary polish. After that point, only release-blocking fixes are allowed.

### Phase C — exact release-candidate verification
Target: **14:30–16:15 JST**

Run on the exact candidate SHA:

1. Cloudflare deployment gate (`build.sh` / `scripts/deploy-gate.mjs`).
2. Complete accumulated v3 architecture validators.
3. Complete accumulated edge/security regressions.
4. Complete browser/mobile Playwright regressions.
5. PWA/offline checks.
6. Relevant accessibility checks.
7. `git diff --check` / syntax/static validation.

Do not transfer PASS across changed SHAs. A failed gate means fix the root cause, create a new candidate, and rerun the affected focused gate plus the complete accumulated suite before release.

### Phase D — release-candidate freeze and production promotion
Target: **16:15–17:00 JST**

1. Freeze the exact clean SHA that passed the complete suite.
2. Record the exact SHA and evidence in `DEVELOPMENT_STATUS_V3.md` and `DEVELOPMENT_HANDOFF_V3.md`.
3. Production promotion is authorized by the user's 2026-09-11 release instruction once the exact candidate is green. No additional permission prompt is required.
4. Promote the verified v3 release state to `main` without mixing unrelated legacy/main work.
5. Do not modify production Supabase/data merely to deploy the static web release unless a verified release blocker specifically requires it.

### Phase E — Cloudflare propagation and production verification
Target: **17:00–17:40 JST**

1. Confirm both Cloudflare Pages projects have deployed the intended `main` state.
2. Confirm canonical host `mybiblequest.pages.dev` loads the v3 release.
3. Confirm compatibility host `biblequest-7th.pages.dev` is not serving a stale incompatible build.
4. Verify core production paths: Home, Account/sign-in surface, Reader, Games, Transform, navigation, PWA/service-worker registration, and a representative mobile-width path.
5. Verify deployment/build identity using available repository/Cloudflare evidence; do not assume a GitHub merge equals propagation.

### Phase F — deadline buffer
Target: **17:40–18:00 JST**

Only fix deployment-blocking issues. If a noncritical issue remains, document it and ship the last verified candidate rather than destabilizing production with a late speculative change.

## User-required tasks

Minimize user involvement. Do not ask the user to repeat repository context, approve routine fixes, or manually perform steps that can be done through the connected tools.

The only expected user-side task, if available, is a final physical-device smoke check after Cloudflare deploys:

- Open `https://mybiblequest.pages.dev/` on the user's Android phone.
- Hard refresh or fully close/reopen the installed PWA.
- Confirm Home renders, Reader opens, one Game launches/returns, and sign-in/account surface is reachable.
- Report only a visible blocker (blank screen, permanent loader, navigation impossible, login impossible, game cannot launch, severe horizontal overflow).

This physical-device check is strongly preferred but does not authorize last-minute feature work. If the user cannot perform it before 18:00, rely on the completed automated/browser gates and production web verification, clearly recording that physical-device acceptance remains unexecuted.

## Stop rules

Until production release is complete, do NOT:

- add new features;
- revive retired Kids/Kana parity rows;
- redesign navigation/layout architecture;
- perform broad refactors without a reproduced release blocker;
- start post-release roadmap work;
- change Supabase schema/data unrelated to a verified release blocker;
- claim a test passed if it was not executed;
- call the app bug-free.

## Success condition

Release is complete when:

- the exact release-candidate SHA passed the required accumulated verification;
- that verified product state is promoted to `main`;
- Cloudflare builds/deploys it to the existing BibleQuest Pages project(s);
- `https://mybiblequest.pages.dev/` serves the intended v3 product and passes production smoke verification;
- any unexecuted physical-device acceptance is explicitly recorded rather than implied.
