(() => {
  'use strict';
  const cfg=window.BQ_CLOUD_CONFIG||{},root=document.getElementById('opsApp'),roleChip=document.getElementById('opsRole');
  const esc=(s='')=>String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[m]));
  let client=null,data=null,filter='all';
  const ago=iso=>{if(!iso)return '—';const n=Math.max(0,Date.now()-new Date(iso).getTime()),m=Math.floor(n/60000);return m<1?'now':m<60?`${m}m ago`:m<1440?`${Math.floor(m/60)}h ago`:`${Math.floor(m/1440)}d ago`};
  const when=iso=>iso?new Intl.DateTimeFormat('en-PH',{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'}).format(new Date(iso)):'—';
  async function invoke(action='dashboard'){const s=(await client.auth.getSession()).data.session;if(!s?.access_token)throw new Error('Sign in again.');const r=await fetch(`${cfg.supabaseUrl}/functions/v1/bq-admin-ops`,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${s.access_token}`,'apikey':cfg.publishableKey},body:JSON.stringify({action})});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||`Request failed (${r.status})`);return j}
  function match(row){return filter==='all'||row.congregation_id===filter}
  function empty(text){return `<div class="ops-empty">${esc(text)}</div>`}
  function summary(){const online=data.online.filter(match),assign=data.assignments.filter(match),messages=data.messages.filter(match),polls=data.polls.filter(match),rooms=data.rooms.filter(match);return `<section class="ops-summary"><div class="ops-stat"><b>${online.length}</b><span>Online now</span></div><div class="ops-stat"><b>${assign.filter(x=>x.active).length}</b><span>Active assignments</span></div><div class="ops-stat"><b>${messages.filter(x=>x.active).length}</b><span>Recent messages</span></div><div class="ops-stat"><b>${polls.filter(x=>x.active).length}</b><span>Persistent polls</span></div><div class="ops-stat"><b>${rooms.filter(x=>x.status!=='ended').length}</b><span>Live rooms</span></div></section>`}
  function online(){const rows=data.online.filter(match);return `<section class="ops-section"><div class="ops-section-head"><div><h2>Who is online</h2><small>Presence expires after about two minutes without a heartbeat.</small></div><b>${rows.length}</b></div><div class="ops-list">${rows.length?rows.map(x=>`<article class="ops-row"><div class="ops-online"><span class="ops-dot"></span><div><h3>${esc(x.display_name||'Member')}</h3><p>${esc(x.congregation_name)} · ${esc(x.role||'member')} · ${esc(x.surface||'BibleQuest')}</p></div></div><small>${ago(x.last_seen_at)}</small></article>`).join(''):empty('No one is currently showing online.')}</div></section>`}
  function assignments(){const rows=data.assignments.filter(match);return `<section class="ops-section"><div class="ops-section-head"><div><h2>Leader / pastor assignments</h2><small>Last 30 days · devotional, reading, guided study, reflection, quiz, group and custom tasks.</small></div><b>${rows.length}</b></div><div class="ops-list">${rows.length?rows.map(x=>`<article class="ops-row"><div><h3>${esc(x.title)}</h3><p>${esc(x.creator_name)} · ${esc(x.creator_role)} · ${esc(x.congregation_name)}</p><div class="ops-tags"><span class="ops-tag">${esc(x.assignment_type)}</span><span class="ops-tag">${esc(x.target_scope)}</span><span class="ops-tag">${x.started} started</span><span class="ops-tag">${x.completed} completed</span>${x.due_at?`<span class="ops-tag">Due ${esc(when(x.due_at))}</span>`:''}${!x.active?'<span class="ops-tag">Archived</span>':''}</div>${x.instructions?`<p>${esc(x.instructions.slice(0,280))}</p>`:''}</div><small>${when(x.created_at)}</small></article>`).join(''):empty('No assignments have been created yet.')}</div></section>`}
  function messages(){const rows=data.messages.filter(match);return `<section class="ops-section"><div class="ops-section-head"><div><h2>Devotionals & messages</h2><small>Congregation announcements, devotionals, activities and encouragement posts.</small></div><b>${rows.length}</b></div><div class="ops-list">${rows.length?rows.map(x=>`<article class="ops-row"><div><h3>${esc(x.title)}</h3><p>${esc(x.creator_name)} · ${esc(x.creator_role)} · ${esc(x.congregation_name)}</p><div class="ops-tags"><span class="ops-tag">${esc(x.message_type)}</span>${!x.active?'<span class="ops-tag">Archived</span>':''}</div><p>${esc((x.body||'').slice(0,320))}</p></div><small>${when(x.created_at)}</small></article>`).join(''):empty('No ministry messages have been published yet.')}</div></section>`}
  function polls(){const rows=data.polls.filter(match);return `<section class="ops-section"><div class="ops-section-head"><div><h2>Persistent congregation polls</h2><small>Separate from one-off Live Room polls.</small></div><b>${rows.length}</b></div><div class="ops-list">${rows.length?rows.map(x=>`<article class="ops-row"><div><h3>${esc(x.prompt)}</h3><p>${esc(x.creator_name)} · ${esc(x.creator_role)} · ${esc(x.congregation_name)}</p><div class="ops-poll">${(x.totals||[]).map(t=>`<div><span>${esc(t.label)}</span><b>${Number(t.total)||0}</b></div>`).join('')}</div><div class="ops-tags"><span class="ops-tag">${x.votes} votes</span><span class="ops-tag">${x.active?'Active':'Closed'}</span>${x.closes_at?`<span class="ops-tag">Closes ${esc(when(x.closes_at))}</span>`:''}</div></div><small>${when(x.created_at)}</small></article>`).join(''):empty('No persistent polls have been created yet.')}</div></section>`}
  function rooms(){const rows=data.rooms.filter(match);return `<section class="ops-section"><div class="ops-section-head"><div><h2>Live rooms & live polls</h2><small>Tracks live-room status, host and participant count.</small></div><b>${rows.length}</b></div><div class="ops-list">${rows.length?rows.map(x=>`<article class="ops-row"><div><h3>${esc(x.title||'BibleQuest Live')}</h3><p>${esc(x.creator_name)} · ${esc(x.creator_role)} · ${esc(x.congregation_name)}</p><div class="ops-tags"><span class="ops-tag">${esc(x.status||'unknown')}</span><span class="ops-tag">${esc(x.state?.activity||'lobby')}</span><span class="ops-tag">${x.participants} participants</span></div></div><small>${when(x.created_at)}</small></article>`).join(''):empty('No live rooms have been hosted yet.')}</div></section>`}
  function render(){roleChip.textContent=data.role==='owner'?'Owner':'Admin';root.className='';root.innerHTML=`<section class="ops-toolbar"><select data-ops-filter><option value="all">All congregations</option>${(data.congregations||[]).map(c=>`<option value="${esc(c.id)}" ${filter===c.id?'selected':''}>${esc(c.name)}</option>`).join('')}</select><button data-ops-refresh>Refresh status</button></section><div class="ops-warning"><b>Usage status</b><div>This page reports actual cloud records. A feature with zero records is implemented but not yet proven by real congregation use.</div></div>${summary()}${online()}${assignments()}${messages()}${polls()}${rooms()}`;root.querySelector('[data-ops-filter]').onchange=e=>{filter=e.target.value;render()};root.querySelector('[data-ops-refresh]').onclick=load}
  async function load(){root.className='ops-loading';root.innerHTML='<div>🐑</div><h1>Refreshing ministry status…</h1>';try{data=await invoke('dashboard');render()}catch(e){root.innerHTML=`<h1>Admin operations unavailable</h1><p>${esc(e.message||String(e))}</p><p><a href="./">Return to BibleQuest</a></p>`}}
  async function boot(){try{if(!cfg.enabled||!window.supabase?.createClient)throw new Error('BibleQuest cloud is unavailable.');client=window.supabase.createClient(cfg.supabaseUrl,cfg.publishableKey,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:false}});const s=(await client.auth.getSession()).data.session;if(!s?.user)throw new Error('Sign in with an Owner/Admin account first.');await load()}catch(e){root.className='ops-loading';root.innerHTML=`<h1>Admin access required</h1><p>${esc(e.message||String(e))}</p><p><a href="./">Return to BibleQuest</a></p>`}}
  boot();
})();